name: Front matter alignment audit

on:
  push:
    branches:
      - align-book-title-pages

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Audit title pages
        run: python3 scripts/align-title-pages.py
